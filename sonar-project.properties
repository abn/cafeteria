# Required metadata
sonar.projectKey=com.github:abn:cafeteria
sonar.projectName=Python Cafeteria
sonar.projectVersion=0.18.0

# Comma-separated paths to directories with sources (required)
sonar.sources=cafeteria

# Language
sonar.language=py

# Encoding of the source files
sonar.sourceEncoding=UTF-8
